  +main() ;creates + calls appropriate method based on input
  -parseInput() ;parses terminal input and checks for invalid input
                ;(may need to make error checking its own function, we'll see)
  +dumpInfo() ;print the appropriate information as specified by terminal input
  +dumpAddress() ;print the IP address
  +dumpPrefix() ;print 1st 3 bytes of the IP address
  +dumpOrg()  ;print the organization associated with the IP address
